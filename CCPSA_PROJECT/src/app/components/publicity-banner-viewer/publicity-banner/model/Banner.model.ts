export class Banner
{
    public idbanner? :number
    public imagen :Blob = new Blob([]);
    public nombreImagen? :string
    public anchoImagen ? :number
    public altoImagen  ? :number
    public tipoArchivo ? :string
}